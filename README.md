# bodega-app
App diseñada para el control del registro de productos en una bodega comercial

Just unzip, go to the unzipped folder directory and do *npm install* (this requires Node.js), and finally *npm run build* after that search for the .exe that generated in the folder
